import { BWorker, IWorker } from './Worker';

export class Bart extends BWorker implements IWorker {
    start(): Promise<null> {
        // TODO -
        return;
    }

    stop(): Promise<null> {
        // TODO -
        return;
    }
}
